# SCsub

Import('env')

env.add_source_files(env.modules_sources, "*.cpp") # Add all cpp files to the build
if env.editor_build:
    env.add_source_files(env.modules_sources, "editor/*.cpp") # Add all cpp files to the build